import styled from 'styled-components'

const StyledAdventuresListPage = styled.div`
    padding: 100px 85px;
`

const StyledAvailableInfo = styled.div`
    font-size: 20px;

    & span {
        color: rgba(100, 100, 100, 1);
    }
`

export { StyledAdventuresListPage, StyledAvailableInfo }
