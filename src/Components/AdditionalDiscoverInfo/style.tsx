import styled from 'styled-components'

const StyledAdditionalDiscoverInfo = styled.div`
    width: 254px;

    & h2 {
        margin-bottom: 24px;
    }

    & span {
        color: rgba(100, 100, 100, 1);
    }
`

const StyledOrangeLine = styled.div`
    color: rgba(252, 69, 0, 1);
`

export { StyledAdditionalDiscoverInfo, StyledOrangeLine }
